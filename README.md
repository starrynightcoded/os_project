# Please check out the branches (treat each branch as a separate folder).

## Compile Branch #2
```bash
g++ sudoko.cpp helper_func.cpp board_25.cpp parallel_solver.cpp -pthread -o a.out
```

## Run
```bash
./a.out
```
